services:
  # cifar10-dataset:
  #   image: fabiocaffarello/cifar10-dataset:latest
  #   container_name: cifar10-dataset
  #   volumes:
  #     - ./data/cifar10:/app/data
  #   environment:
  #     - PYTHONUNBUFFERED=1
  #   healthcheck:
  #     test: ["CMD", "test", "-f", "/app/data/processing_complete"]
  #     interval: 90s
  #     timeout: 5s
  #     retries: 5

  # py-cnn-benchmark:
  #   image: fabiocaffarello/py-cnn-benchmark:latest
  #   container_name: py-cnn-benchmark
  #   volumes:
  #     - ./data/cifar10:/app/data
  #   depends_on:
  #     cifar10-dataset:
  #         condition: service_healthy
  #   environment:
  #     - PYTHONUNBUFFERED=1

  go-cnn-benchmark:
    image: fabiocaffarello/go-cnn-benchmark:latest
    container_name: go-cnn-benchmark
    volumes:
      - ./data/cifar10:/app/data
    # depends_on:
    #   cifar10-dataset:
    #       condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1